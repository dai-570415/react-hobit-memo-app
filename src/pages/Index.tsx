import { Layout } from '../components/Layout/Layout';
import { Memo } from '../components/Memo/Memo';

export const Index = () => {
    return (
        <Layout>
            <Memo />
        </Layout>
    );
}